Boolean("0");
// returns true because "0" is a non-empty string

Boolean("");
// returns false because "" is a empty string

Boolean(0);
// returns false because 0 is falsy value

Boolean(null);
// returns false because null is falsy value