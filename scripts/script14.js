// difference between null and undefined

// when we assign a value "null" to a variable, we are purposely setting the value as null to the variable. 
// whereas in case of undefined, we didn't assign any value to the variable.