console.log(0.1+0.2===0.3); //gives false

// this happens because js stores number in binary format. in that case, the addition of binary numbers of 0.1 and 0.2 is slightly greater than 0.3. so it results in false