// in operator

let user = {name : "peter", age : 44};
console.log("name" in user)     //output : true, since the key is present in user

let arr = [2,3,4,5];
console.log(1 in arr) //output : true, since index exists

