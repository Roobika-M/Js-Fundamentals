let num = 5;
let str = "5";

console.log(num == str);  // Outputs: true 
console.log(num === str); // Outputs: false 

//  == checks only the value and not datatype
// === checks both value and datatype