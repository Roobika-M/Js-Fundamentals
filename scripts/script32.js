let a = true;
let b = true;
let c = false;

console.log(a && b && c);  // false
console.log(a || b || c);  // true

// for AND operator, all conditions should be true to return true
// for OR operator, at least one condition should be true to return true