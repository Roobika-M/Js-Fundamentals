alert("hello all");

"when the script tag is placed before the body tag, the content will be loaded before the html body content is rendered, so it won't execute"

"when the script tag is placed after body tag, the script won't be executed"