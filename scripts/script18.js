// in case of prompt, its either a value (which is string) or null

let num = prompt("enter a number");
alert(num);
// in the above code when user enters a value and click ok, the number is displayed
// if user clicks cancel, null is displayed

let con = confirm("are you sure?");
alert(con);
// in the above code when user clicks ok, true is displayed
// if user clicks cancel, false is displayed